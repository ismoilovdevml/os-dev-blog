import Card, { Cards } from '@components/core/Tutorial/Card';
import developingGuides from './article/_meta.en-US.json';

## Computer Science bo'yicha barcha maqolalar

Shu paytgacha Computer Science bo'yicha yozib kelingan barcha blog post va maqolalar jamlanmasi.

<Cards
  num={1}
  children={Object.keys(developingGuides)
    .filter((key) => developingGuides[key].category === 'developing')
    .map((key, i) => (
      <Card
        key={i}
        title={developingGuides[key].longTitle}
        level={developingGuides[key].level}
        category={developingGuides[key].category}
        author={developingGuides[key].author}
        createdAt={developingGuides[key].createdAt}
        minutesRead={developingGuides[key].minutesRead}
        href={developingGuides[key].href}
      />
    ))}
/>
